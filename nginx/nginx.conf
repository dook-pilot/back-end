; http {
;     # ...
;     upstream api {
;         server 127.0.0.1:8000;
;     }

;     server {
;         listen 80;
;         server_name 18.184.132.164;
;         root /usr/src/app;

;         location / {
;             index index.html;
;         }

;         location /static/  {
;             alias /usr/src/app/static/;
;         }

;         location /main {
;             include /etc/nginx/uwsgi_params;
;             uwsgi_pass api;
;             uwsgi_param Host $host;
;             uwsgi_param X-Real-IP $remote_addr;
;             uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
;             uwsgi_param X-Forwarded-Proto $http_x_forwarded_proto;
;         }
;     }
; }


# mysite_nginx.conf

# the upstream component nginx needs to connect to
upstream api {
    # server unix:///path/to/your/mysite/mysite.sock; # for a file socket
    server 127.0.0.1:8000; # for a web port socket (we'll use this first)
}

# configuration of the server
server {
    # the port your site will be served on
    listen 8000;
    # the domain name it will serve for
    server_name 18.184.132.164; # substitute your machine's IP address or FQDN
    charset utf-8;

    # max upload size
    client_max_body_size 75M;   # adjust to taste

    location /static {
        alias /usr/src/app/static/; # your Django project's static files - amend as required
    }

    # Finally, send all non-media requests to the Django server.
    location / {
        uwsgi_pass  api;
        include     /usr/src/app//uwsgi_params; # the uwsgi_params file you installed
    }
}